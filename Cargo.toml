[package]
name = "todo-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
chrono = "0.4.43 "
serde = {version = "1.0.22", features = ["derive"]}
serde_json = "1.0.14"
clap = {version = "4.5.60", features = ["derive"]}
dirs = "6.0.0"
